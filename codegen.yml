overwrite: true
schema: "http://localhost:4000/graphql"

documents:
  - "./src/graphql/**/*.graphql"
  # - "./src/graphql/mutations/*.graphql"
  # - "./src/graphql/queries/*.graphql"

generates:
  src/generated/graphqlnew.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
  # ./graphql.schema.json:
  #   plugins:
  #     - "introspection"
